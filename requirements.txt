# ΨBot Dependencies — CCDR Expectation Field Architecture v1.0

# Core numerical
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0

# Data validation and state
pydantic>=2.0.0

# Structured logging
structlog>=23.0.0

# Configuration
pyyaml>=6.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Statistical tests (for hypothesis backtester)
statsmodels>=0.14.0       # Granger causality tests (T1)

# Optional: dip test for T3 bimodality
# diptest>=0.4.0          # install separately: pip install diptest

# Optional: GPU-accelerated Dupire PDE (Phase 1+ performance)
# cupy-cuda12x>=12.0.0    # for NVIDIA GPU
# torch>=2.0.0            # alternative GPU backend

# Optional: QuantLib for PDE solver
# QuantLib-Python>=1.31

# Optional: broker integration (Phase 3)
ib_insync>=0.9.70         # Interactive Brokers TWS API via IB Gateway (Phase 3)

# Optional: monitoring (Phase 3)
# prometheus-client>=0.17.0
# grafana-api>=1.0.3

# Data feeds (Phase 0 backtesting — required for hypothesis tests)
yfinance>=0.2.28
# pandas-datareader removed — broken with pandas 2.x; French data fetched directly
fredapi>=0.5.0             # FRED macroeconomic data (T2, T4, T6) — requires FRED_API_KEY
openpyxl>=3.1.0            # Shiller Excel download (T6)
requests>=2.31.0           # CBOE + FINRA HTTP fetchers (T1, T5, T8)
pyarrow>=12.0.0            # parquet cache for data fetchers

# Optional: improved bimodality test for T3
# diptest>=0.4.0           # install separately: pip install diptest
